apiVersion: 1
datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    isDefault: true
    url: postgres:5432
    database: energy_db
    user: airflow
    secureJsonData:
      password: airflow
    jsonData:
      sslmode: "disable" 